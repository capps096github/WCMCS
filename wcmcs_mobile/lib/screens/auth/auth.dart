import '../../wcmcs_exporter.dart';
///class for authentication
class WcmcsAuth extends StatelessWidget {
  ///[WcmcsAuth] constructor
  const WcmcsAuth({super.key});

  ///this i guesss is the login page or the authentication page file with loging in details abput every thing
  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
