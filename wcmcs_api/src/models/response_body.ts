
export interface ResponseBody {
    status: string;
    controller: number;
    message: string;
}

// const responseBody = {
//     status: "success",
//     controller: section.controller,
//     message:
//         `Controller value for section ${section.label} is ${section.controller}`,
// };
